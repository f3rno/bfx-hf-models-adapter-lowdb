'use strict'

/**
 * `lowdb` database instance, used as `db` object for the provided `DBAdapter`
 *
 * See the
 * {@link https://github.com/typicode/lowdb/blob/master/README.md|lowdb README}
 * for available methods.
 *
 * This type is a stub to link to the documentation above; all `lodash` methods
 * are available for manipulation of data.
 *
 * @private
 * @typedef {object} LowDBInstance
 * @property {Function} get - getter
 * @property {Function} set - setter
 */
